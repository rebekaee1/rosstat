# США: полные быстрые страницы и локальная проверка — 24.09.2026

Проверка велась в локальной БД и на `127.0.0.1:3000`. Продакшн не менялся.

## Быстрые страницы

- США: 1 886 канонических национальных рядов с фактическими данными; 60 338 сочетаний ряд × наблюдаемый год.
- 50 штатов и округ Колумбия: 1 882 доступных показателя, 95 922 сочетания территория × показатель с числовыми фактами и 3 012 854 сочетания территория × показатель × наблюдаемый год. У 60 территориальных сочетаний числовых данных нет (55 подавленных/отсутствующих BEA и 5 рядов минимальной оплаты); пустых годовых страниц для них нет.
- Для каждой существующей карточки годовые ссылки строятся из фактических дат, без фиксированного списка годов или выборочных показателей. У каждого годового URL отдельная OG-картинка и видимый график; год без наблюдений возвращает 404.
- Для 51 территории предусмотрено 1 275 канонических пар сравнения. Обратный порядок пары перенаправляется на канон. Годовые ссылки штатов и национальных рядов публикуются в чанках sitemap не более 10 000 URL.
- Годовая страница месячного или квартального ряда явно показывает неполный год; изменение к прошлому году сравнивает одинаковый месяц или квартал. У национального ряда в сопоставлении своя единица измерения.
- Локальная статическая генерация sitemap завершилась: 5 487 486 URL в 559 секциях, включая все 302 чанка годовых страниц штатов (последний — 2 854 URL) и 1 275 пар сравнения. Генерация `281b9c32d4134bae81850ea3673975b4`, завершение 17:15:36 UTC; состав мирового каталога продолжает меняться при загрузке Eurostat.

## История и источники

- Сверка с первоисточниками выявила три ошибки в национальных данных США: единицы `ICSA`, масштаб `IEABC` и базу индекса `USSTHPI`. Они исправлены и повторно сверены по официальным CSV. Для 51 территории актуализированы единицы ряда медианного дохода; августовские разрешения на строительство загружены.
- Проверены определения всех 1 866 региональных рядов BEA против официальных архивов. Дополнительная независимая потоковая сверка закрыла все 41 таблицу действующего каталога из семи ZIP: 4 305 273/4 305 273 опубликованных дат и значений США и штатов совпали без пропусков, лишних точек или численных расхождений. Архивные 1 456 нулевых значений снятого с листинга ряда отделены от текущего каталога. Полная проверка всех наборов Eurostat ещё продолжается.
- Старый месячный HICP Eurostat прекращён после 2025 года. Активный индекс переведён на `prc_hicp_minr` с историей с января 1996 года; 36 старых карточек ведут 301 на новые, архивные США/Великобритания остаются доступны. Официальные источники и границы проверки описаны отдельно в `us-eu-source-audit-2026-09-24.md`.

## Наши прогнозы

- Общий для США и Европы v2 quality gate проверяет весь горизонт rolling-origin, считает MASE только по обучающей части и обнаруживает ревизии значений внутри истории. Старые v1 прогнозы и прогнозы Европы, для которых источник ещё не обновлён, не показываются.
- В локальной БД сейчас 179 опубликованных v2 национальных прогнозов США и 652 по Европе. Последний batch Европы: 620 прошли, 196 не прошли, 0 ошибок. Для 831 опубликованного прогноза проверены 4 496 точек: нарушений границ интервалов, пересечения прогноза с фактом и неверной длины горизонта нет.
- Из 51 проверенного прогноза реального ВРП территорий 15 прошли, 36 не прошли; отказ честно скрывает прогноз. Официальный пробел BLS за октябрь 2025 года не заполнялся выдуманными данными. Детали — `world-forecast-v2-2026-09-24.md`.
- Повторный запрос текущего state forecast API: реальный ВРП Калифорнии доступен (MASE 0,876 против 1,624; 4 окна); Техас скрыт (MASE 1,973); безработица CA/TX скрыта с `recent_history_gap`. Все четыре ответа имеют валидную структуру; опубликованный прогноз CA не выходит за интервальные границы. Это подтверждает fail-closed поведение для непрошедших рядов.

## Обновления, Telegram и интерфейс

- US FRED/BLS опрашиваются ежедневно, BEA regional ZIP — еженедельно, Eurostat API — дважды в день после его окон обновления. Первые локальные сообщения по США и Европе отправлены через существующий Telegram outbox и подтверждены как доставленные; частичный охват в них обозначен.
- Live batch Eurostat #28 обработал 200/200 наборов без ошибок, но завершился со статусом `partial` из-за оставшейся очереди 1 649 наборов. Telegram outbox id=1162 доставил технический отчёт. Отдельный локальный Docker `rosstat-eurostat-backfill-20260924` обрабатывает очередь последовательными партиями по 200 с Redis lock/heartbeat; #29 и #30 начали обработку без повторения успешных наборов предыдущих запусков. Ночной cron продолжит очередь после остановки фонового процесса. Публикация европейского прогноза ждёт успешного обновления его набора.
- Главная берёт числа из API; устаревшие запасные значения удалены. В описании США указаны 50 штатов и округ Колумбия. Число рядов мира будет расти по мере завершения Eurostat batch.
- В карточке любого национального показателя США ссылки показывают каждый наблюдаемый год; в карточке показателя штата удалено ограничение «последние 10 / каждый пятый». Профиль штата позволяет выбрать любой из 1 882 показателей и показывает все его наблюдаемые годы. В браузере у невыделенного показателя Калифорнии `bea-sagdp3-53` видны все 28 ссылок с 1997 по 2024 год; у национальной безработицы — с 1948 по 2026 год.
- У сравнительной OG-картинки светлая подложка под таблицей и заголовком. Вручную осмотрены Калифорния/Техас, Северная/Южная Каролина, годовая картинка Калифорнии и длинное название BEA: текст читается поверх декоративного фона.
- Дополнительно через фактический шрифт и алгоритм компоновки проверены все 1 882 русских заголовка показателей штатов, 1 936 листинговых национальных заголовков США (включая ряды без канонической страницы с фактами) и все 1 275 заголовков пар штатов: превышений отведённой высоты — 0. Это проверка геометрии текста; визуальные контрольные изображения указаны выше.
- После пересборки точной локальной версии readiness отвечает 200; migration-drift check сообщает 0 структурных расхождений и 33 безвредных отличия. `scripts/e2e/smoke.mjs` прошёл 5 browser-сценариев и 5 YandexBot SSR-страниц без JS-ошибок. Страница регистрации в английском UI сохраняет Google-кнопку и переводит к email-форме с временным сообщением.
- Через Playwright непрерывное перетаскивание американской карты 2023 → 2011 обновило URL и значение ползунка; все 11 запросов карты ответили 200. Национальная безработица США, штатные страницы CA, год CA, сравнение CA/TX и sitemap чанки `world-region-years-1`/`world-region-vs` отвечают 200.
- После исправления устаревшего тестового ожидания прошёл весь `./scripts/check-all.sh`: **2 868 backend-тестов passed, 8 skipped**, **872 frontend-теста passed**, lint и сборка фронтенда, актуальность индекса индикаторов, счётчики документации, page-meta и аудит публичного языка. `git diff --check` чист. У backend 66 предупреждений, главным образом от библиотек и старых тестов; ошибок нет. Индекс индикаторов перегенерирован штатным скриптом.

## Границы приёмки

Проверка не доказывает абсолютную корректность каждой будущей публикации. Статическая публикация sitemap завершена локально; полный догон каталога Eurostat ещё выполняется. Для продакшна потребуется отдельная приёмка и одобренный деплой.
