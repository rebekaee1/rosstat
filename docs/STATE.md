# STATE — где остановились (handoff для следующего агента)

**Обновлено:** 2026-09-26, ~13:30 МСК. **Правило:** каждый агент в конце работы
переписывает этот файл: факты с SHA, без истории (история — в `git log`).
Держать коротким (≤ ~60 строк); закрытое удалять.

## Прод

- На проде `41539a9` (деплой 2026-09-26 12:43–13:05 МСК, watch 15 мин зелёный).
  Внутри: ускорение фронта (precompress/gzip_static), убрана доверительная полоса
  прогноза, sitemap без `<lastmod>` до 1970, фикс пула БД (`7c076d4`: SSR отпускает
  соединение до ожидания кэша, пул 10+15, timeout 3 с, деплой чистит только
  `fe:*:ssr:*`), внешние ссылки на источники (`4bb32ae`, `SourceLink`).
- Предыдущая попытка `cd55001` (11:24) откатилась на 12-й минуте watch — разбор:
  CONTEXT, Cold-cache pool exhaustion trap.

## В работе

- **Perf batch 2** — локальная ветка `perf-batch2` в основной копии
  `/Users/iprofi/tradingeconomics/rosstat` (другой агент, изменения не закоммичены —
  не переключать ветку, не stash, не править там файлы). Состав: web 3 воркера +
  планировщик отдельным процессом; переписывание тяжёлых запросов (окна страницы
  страны, `cat_siblings` мировых индикаторов); фикс частичного индекса
  `ix_world_indicators_listed_signal` (`.is_(True)` → `IS true`, индекс не
  используется) + кэш главной; OG отпускает сессию БД до рендера; сжатие SSR-кэша
  (zlib ~6×); тяжёлые джобы вне event loop; `$request_time` в логах nginx.
- **Perf batch 3 — ждёт** (нужна ~1 мин остановки БД): память/настройки Postgres
  (контейнер 2,5 ГБ, shared_buffers 640MB, jit on) + `pg_stat_statements`;
  удалить `fe_sync_*_20260924` (~1,6 ГБ) и дубль-индекс
  `ix_world_data_points_indicator_date` (471 МБ).
- Замеры и обоснование — [`backlog.md`](backlog.md), раздел 2026-09-26.

## Открыто

- Ежедневный ETL 06:00/20:00: 107 из 117 загрузок падают — диагностируется (свежесть данных).
- UptimeRobot — аккаунт заводит владелец.
- Лимит PerplexityBot подготовлен, не включён: `deploy/optional/perplexitybot-ratelimit.nginx.conf`.
- IndexNow-подача каталога — ждёт решения: [`indexnow-sitemap-backfill-plan.md`](indexnow-sitemap-backfill-plan.md).
- `/world/united-states/gdp` → 301 на `/united-states/indicator/gdp` → 404.
- Невлитая ветка `origin/cursor/prod-hardening-audit-6686` (1 коммит `f70f2ba`: деплойные 502 + алерты) — решить: влить или закрыть.
- `/methodology`: абзац про доверительную полосу устарел (полосы больше нет) — убрать RU/EN.

## Рабочий режим сейчас

- Деплои — пачками по явному «деплой до `<sha>`» (approval-only коммит в
  `deploy/approved-shas.txt`), не в 03:40–04:00 МСК. Процедура — [`workflow.md`](workflow.md#прод-деплой).
- Бэкапы: сервер 07:00 МСК, Mac тянет 08:15 (проверено 2026-09-26: sha256 и
  `pg_restore --list` ок). Восстановление не отрабатывалось.
